# homebrew-tools

## Usage

Using `brew tap`:

    brew tap SEEK-Jobs/homebrew-tools
    brew install gantry
    gantry --version

Directly:

    brew install SEEK-Jobs/homebrew-tools/gantry
    gantry --version